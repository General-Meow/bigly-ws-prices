const assert = require('assert')

describe("some overall thing like a class", () => {
  describe("some method", () => {
    it("should return true when blah", () => {
      assert.equal(true, true)
    })
  })
})